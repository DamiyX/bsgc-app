# Project-specific R8 rules.
#
# Flutter plugins and Firebase publish consumer rules through their Android
# libraries. Keep this file intentionally small; add a rule only when a
# release-only failure is reproduced and covered by a test.

-keepattributes SourceFile,LineNumberTable
